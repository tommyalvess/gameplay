const DB_NAME = '@gameplay'

//@app:colletion - melhor organizar
const COLLECTION_USERS = `${DB_NAME}:user`
const COLLECTION_APPOINTMENTS = `${DB_NAME}:appointments`


export {
    COLLECTION_USERS,
    COLLECTION_APPOINTMENTS
}